apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-app-example.fullname" . }}
  labels:
    {{- include "fastapi-app-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-app-example.selectorLabels" . | nindent 4 }}
